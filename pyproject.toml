[tool.poetry]
name = "pre-commit-test"
version = "0.1.0"
description = ""
authors = ["alexandre duverger <alexandre.duverger@proton.me>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
